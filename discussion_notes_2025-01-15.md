# 事实核查系统架构讨论记录
*日期：2025-01-15*

## 参与文档
- `reference_doc.md` - 理想化的金融数据验证技术方案（16周实施，五层架构）
- `readme_version_1.md` - 已实施的通用事实核查脚手架（轻量级 ReAct）

## 核心讨论议题

### 1. 架构对比与选择

#### Reference Doc (理论方案)
- **优势**：理论完备，准确率极高（90%+ F1），完整的不确定性量化
- **劣势**：过度工程化，16周实施周期，强供应商锁定，维护成本高

#### README v1 (实施方案) ✅ **推荐**
- **优势**：架构清晰，去耦合设计，4周可上线 MVP，可插拔扩展
- **劣势**：缺少高级特性（多模型集成、不确定性量化）

**结论**：从 README v1 起步，渐进式添加 Reference Doc 的精华部分

### 2. 关键洞察：ReAct 架构天然避免 LLM 幻觉

用户的核心观察：
> "我是用搜索结果来验证事实，而并不是使用 LLM 自身具有的知识来验证事实"

这从根本上改变了问题性质：
```
传统模式：LLM 生成事实 → 幻觉风险高
README v1：LLM 编排搜索 → 工具返回事实 → LLM 总结证据 → 幻觉风险极低
```

### 3. 五个核心技术的重新评估

| 技术 | 原始用途 | README v1 下的价值 | 优先级 |
|------|----------|-------------------|---------|
| **提示缓存** | 成本优化 | 不变，节省 84% 成本 | ⭐⭐⭐⭐⭐ 必须 |
| **Chain-of-Verification** | 防止事实幻觉 | 改为验证推理正确性 | ⭐⭐⭐ 中等 |
| **多采样一致性** | 检测事实幻觉 | 检测查询偏差、推理稳定性 | ⭐⭐⭐ 中等 |
| **温度缩放校准** | 校准记忆置信度 | 校准判断置信度 | ⭐⭐⭐⭐ 高 |
| **三层解释系统** | 建立信任 | 更重要（ReAct 产生丰富过程数据） | ⭐⭐⭐⭐ 高 |

### 4. README v1 架构下的剩余挑战

虽然事实编造被消除，但仍存在：
1. **推理错误**：计算、百分比等数学运算错误
2. **选择偏见**：选择性忽略反面证据
3. **语义曲解**：misinterpret 搜索结果含义
4. **查询生成偏差**：搜索查询本身有倾向性

### 5. 改进建议（基于 README v1）

#### Phase 1: MVP (4周)
- 保留轻量级 ReAct 循环
- 实现工具插件接口
- Docker + CLI 部署

#### Phase 2: 推理增强 (2周)
```python
class ReasoningValidator:
    def validate_math()     # 验证计算
    def check_completeness() # 检查证据完整性
    def detect_qualifier_loss() # 检测限定词丢失
```

#### Phase 3: 搜索优化 (2周)
```python
def generate_balanced_queries(claim):
    # 生成正面查询
    # 生成反面查询
    # 生成中立查询
```

#### Phase 4: 成本优化 (2周)
- Claude 3.7 提示缓存
- 批处理 API
- Redis 查询缓存

### 6. 技术细节解释

#### 提示缓存
- 将系统指令、工具定义等标记为可缓存
- 5分钟内重复调用仅收 10% 费用
- 1000条批处理：$21 → $3.4

#### 多采样一致性
- 采样 3-5 次，计算答案一致性
- 一致性低的自动标记人工审核
- 成本 3-5x，但配合缓存可控

#### Chain-of-Verification (简化版)
```python
def verify_reasoning(evidence, conclusion):
    # 不验证事实，验证逻辑
    return llm("验证计算正确性、逻辑严密性、限定词完整性")
```

#### 温度缩放校准
- 收集 200 样本验证集
- 学习缩放参数 T
- 将 raw_confidence 转为 calibrated_confidence
- ECE 从 0.18 降至 0.04

#### 三层解释系统
- **用户层**：简洁结论 + 关键证据
- **分析师层**：完整推理链 + 所有证据
- **审计层**：工具调用轨迹 + 敏感性分析

## 实施路线图

```
Week 1-4:  README v1 MVP
Week 5-6:  推理验证器 + 平衡搜索
Week 7-8:  提示缓存 + 成本优化
Week 9-12: 金融专用插件包（可选）
```

## 核心原则
**"先做对（清晰接口），再做好（准确率优化）"**

## 下次讨论议题
1. 推理验证器的具体实现
2. 证据冲突处理机制
3. 搜索策略优化算法
4. 金融场景特定增强
5. 批处理与并发策略

## 关键决策记录
- ✅ 选择 README v1 架构作为基础
- ✅ 优先实施提示缓存（成本必须）
- ✅ CoV 改为推理验证而非事实验证
- ✅ 三层解释系统从 v1.1 开始实施
- ❌ 不采用 MCP（过于超前）
- ❌ 不采用 LangChain 复杂编排（过重）
- ❌ 不实施 SPUQ/保形预测（学术性过强）